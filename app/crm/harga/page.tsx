import Link from "next/link";import {Header} from "@/components/Header";import {Footer} from "@/components/Footer";export const metadata={title:"Harga — Seara CRM"};
const plans=[["Starter Tenant","Rp499K","Core CRM|Omnichannel basics|Lead management|Basic analytics",false],["Pro Tenant","Rp1.49JT","Advanced omnichannel|Auto assignment|Round robin|Multi-unit|Advanced analytics",true],["Enterprise","Custom","Multi-tenant|Advanced access|API & webhook|Custom integration|Priority support",false]];
export default function Page(){return <><Header crm/><main><div className="wrap heroSingle"><span className="eyebrow">PRICING</span><h1>Choose the right <span className="gradient">CRM capacity.</span></h1><p className="lead">Pricing ini mengikuti informasi yang digunakan pada prototype; pastikan nominal dan limit final sebelum production.</p></div><section><div className="wrap priceGrid">{plans.map(([name,price,features,featured])=><article className={`priceCard ${featured?"featured":""}`} key={name as string}>{featured&&<span className="eyebrow">MOST POPULAR</span>}<h3>{name}</h3><div className="price">{price}<small>{price!=="Custom"?" / bulan":""}</small></div><ul className="checks">{(features as string).split("|").map(f=><li key={f}>{f}</li>)}</ul><Link className={featured?"btn btnBlue":"btn btnGhost"} href="/crm/contact">{price==="Custom"?"Talk to Sales":"Request Demo"}</Link></article>)}</div></section></main><Footer crm/></>;}
